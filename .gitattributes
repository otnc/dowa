# Normalize all text files to LF, regardless of the platform or the
# contributor's core.autocrlf setting, so Prettier (which writes LF) never
# disagrees with what's checked out on Windows.
* text=auto eol=lf

test/** linguist-vendored
